# VOXERA 2026 — Production Upgrade Todo

> Generated by code audit of the existing codebase against PRD.md, TECH_RULES.md, and DESIGN.md.
> Tasks are ordered by severity. Do not rebuild from scratch — improve only.
> Mark tasks `[x]` when done, `[/]` when in progress.

---

## 🔴 PHASE 1 — Critical Blockers (Must Fix Before Any Launch)

These are launch-blocking bugs and security holes. Fix these first.

### 1.1 Security — Admin Password Exposure
- [ ] **[C-02]** Remove hardcoded password `'JBCLUB@10102026'` from `src/pages/Admin.tsx` (line 54)
- [ ] Create `POST /api/auth/login` endpoint that validates against a bcrypt hash stored in env var `ADMIN_PASSWORD_HASH`
- [ ] Return a signed JWT (24h expiry) on successful login; store in `httpOnly` cookie (not localStorage)
- [ ] Add `verifyAdmin` middleware that checks the JWT on all admin-only API routes
- [ ] Create new page `src/pages/AdminLogin.tsx` — full-screen login form, no layout wrapper
- [ ] Create `src/store/authStore.ts` (Zustand) to hold `isAuthenticated` state client-side
- [ ] Create `src/components/ProtectedRoute.tsx` that redirects to `/admin/login` if not authenticated
- [ ] Update `src/App.tsx` to wrap `/admin` route with `<ProtectedRoute>`
- [ ] Add route `/admin/login` to `src/App.tsx`
- [ ] Install packages: `npm install bcryptjs jsonwebtoken @types/bcryptjs @types/jsonwebtoken`

### 1.2 Database — Migrate from SQLite to PostgreSQL
- [ ] **[C-04]** Install Drizzle ORM + PostgreSQL driver: `npm install drizzle-orm pg && npm install -D drizzle-kit @types/pg`
- [ ] Create `server/db/schema.ts` with Drizzle schema for tables: `events`, `registrations`, `settings`, `contact_submissions`, `admin_users`
  - All monetary values stored as INTEGER in paise (fee × 100)
  - All tables have `created_at` and `updated_at` timestamps
  - Events table has `deleted_at` for soft deletes
  - Registrations table has `registration_code VARCHAR(20)`, `payment_status`, `payment_id`, `college_name`, `department`, `year_of_study`
  - Events table has: `registration_fee_single INTEGER`, `registration_fee_team INTEGER`, `team_size_min`, `team_size_max`, `slots_total`, `slots_filled`, `google_form_url`
- [ ] Create `server/db/index.ts` with Drizzle + pg pool using `process.env.DATABASE_URL`
- [ ] Add `DATABASE_URL` to `.env.example`
- [ ] Run `npx drizzle-kit push` against a Supabase (or local PostgreSQL) instance
- [ ] Rewrite all queries in `server.ts` to use Drizzle ORM (no raw SQL, no SQLite)
- [ ] Remove `better-sqlite3` dependency and `db.ts` after migration
- [ ] Seed the 6 events with all new fields populated (including correct INR fees)

### 1.3 Registration Fees — Fix Currency and Add Fee Logic
- [ ] **[C-05] [H-04]** Fix all prize amounts in `src/data/events.ts` from `$` (USD) to `₹` (INR)
  - `debate-competition`: prize `₹5,000`
  - `poetry-reciting`: prize `₹3,000`
  - `pitch-perfect`: prize `₹10,000`
  - `open-mic`: prize `Goodies + Certificate`
  - `treasure-hunt`: prize `₹8,000`
  - `film-screening`: prize `Best Film Trophy + Certificate`
- [ ] Create `src/constants/fees.ts` — client-side fee reference (display only, not trusted for payment)
- [ ] Create `server/utils/fees.ts` — authoritative server-side fee calculation (as specified in TECH_RULES §4.4):
  ```
  debate-competition: single=99, team=150, maxTeam=2
  treasure-hunt: team=120 (fixed), maxTeam=4
  pitch-perfect: flat=50, maxTeam=4
  open-mic: single=99, team=150, maxTeam=2
  poetry-reciting: perPerson=99, individual only
  film-screening: perPerson=80, maxTeam=10
  ```
- [ ] Add `team_size_min` and `team_size_max` to event data model and DB schema
- [ ] Update `VoxeraEvent` interface in `src/data/events.ts` to include all new fields from PRD §5.2.1

### 1.4 Schedule Page — Fix Static Data Bug
- [ ] **[C-06]** Replace `import { events } from '../data/events'` in `src/pages/Schedule.tsx` with a live API call `fetch('/api/events')`
- [ ] Add loading and error states to Schedule page
- [ ] Add Day 1 / Day 2 filter toggle (March 16 and March 17)

---

## 🟠 PHASE 2 — High Priority (Must Fix Before Soft Launch — March 1)

### 2.1 Payment Gateway — Razorpay Integration
- [ ] **[C-01]** Install: `npm install razorpay && npm install react-razorpay` (or use the Razorpay JS global script)
- [ ] Add env vars to `.env.example`: `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `RAZORPAY_WEBHOOK_SECRET`
- [ ] Create `POST /api/orders` endpoint:
  - Accepts `{ event_id, team_size }`
  - Calls `calculateFee(event_id, team_size)` **server-side**; never trust client amounts
  - Creates Razorpay order via SDK, returns `{ order_id, amount, currency, key_id }`
- [ ] Create `POST /api/webhooks/razorpay` endpoint:
  - Verifies HMAC-SHA256 webhook signature using `RAZORPAY_WEBHOOK_SECRET`
  - On `payment.captured`: updates registration `payment_status = 'confirmed'`, stores `payment_id`
  - Sends confirmation email
- [ ] Rework `src/pages/Register.tsx` into a 3-step form:
  - Step 1: Event selection (pre-populated from `?event=` query param) + team size
  - Step 2: Participant details — Name, Email, Phone, College, Department, Year; + team member names/emails
  - Step 3: Fee review + consent checkbox → trigger Razorpay modal
- [ ] Add `src/lib/razorpay.ts` helper for opening the Razorpay modal
- [ ] On payment success, `POST /api/registrations` with form data + `payment_id + order_id`
- [ ] Create new page `src/pages/RegisterSuccess.tsx`:
  - Shows Registration ID (VX-2026-XXXX), event name, date, venue, team, amount paid
  - "Add to Google Calendar" link
  - "Share on WhatsApp" link

### 2.2 Email Confirmation
- [ ] **[C-03]** Install: `npm install resend`
- [ ] Add `RESEND_API_KEY` and `FROM_EMAIL` to `.env.example`
- [ ] Create `server/services/email.service.ts`:
  - `sendConfirmationEmail(registration)` — sends branded HTML email after payment confirmed
  - Email subject: `✅ You're registered for [Event] — VOXERA 2026!`
  - Email body includes: Registration ID, Event, Date, Venue, Team, Amount Paid, Rules reminder, contact info
- [ ] Trigger email from the Razorpay webhook handler on `payment.captured`
- [ ] Add admin email notification for new contact form submissions

### 2.3 New Pages
- [ ] **[H-03]** Create `src/pages/NotFound.tsx`:
  - Full branded 404 page with VOXERA logo, message, and "Go Back Home" button
  - Add `<Route path="*" element={<NotFound />} />` to `App.tsx`
- [ ] Create `src/pages/About.tsx` (`/about`):
  - Club history, team members grid, past editions, gallery placeholder
- [ ] Add `/about` to `Navbar.tsx` nav links
- [ ] Add `/about` route to `App.tsx`

### 2.4 Home Page Missing Sections
- [ ] **[H-02]** Add Countdown Timer to hero section in `src/pages/Home.tsx`:
  - Target: March 16, 2026 00:00:00 IST
  - Create `src/hooks/useCountdown.ts` custom hook
  - Display: Days / Hours / Minutes / Seconds with animated digit blocks
  - Mobile: 2×2 grid layout; Desktop: 4-column row
- [ ] Add hero dual CTAs: "Register Now" (primary violet gradient) + "Explore Events" (secondary outline)
- [ ] Add Stats Strip section: animated counters for `6 Events`, `500+ Participants`, `₹26,000+ Prize Pool`
  - Use `react-countup` or build a simple scroll-triggered counter
- [ ] Add About VOXERA section (2-column: text + highlights list)
- [ ] Add Final CTA Block: "Register Before Slots Fill Up" with urgency feel
- [ ] Add Footer section to `src/components/Footer.tsx` (currently incomplete):
  - Nav links, social icons (from settings API), contact info, copyright
  - Replace placeholder `#` hrefs with real links from settings

### 2.5 Mobile Navigation
- [ ] **[H-01]** The hamburger toggle exists in `Navbar.tsx` but the mobile dropdown does not render nav links
  - Verify `AnimatePresence` + mobile dropdown actually renders links on small screens
  - Fix if broken; add full-screen overlay style on mobile (slide-down, stagger links)

### 2.6 SEO Metadata
- [ ] **[H-06]** Install: `npm install react-helmet-async`
- [ ] Wrap app in `<HelmetProvider>` in `src/main.tsx`
- [ ] Create `src/lib/seo.ts` helper
- [ ] Add `<Helmet>` with page-specific `<title>`, `<meta name="description">`, Open Graph tags, Twitter Card tags to every page
- [ ] Add OG banner image `public/og-banner.jpg` (1200×630px)
- [ ] Update `index.html` with canonical link, lang attribute `en`, Google Fonts links (Inter, DM Sans, JetBrains Mono)
- [ ] Add `public/robots.txt` and `public/sitemap.xml`

---

## 🟡 PHASE 3 — Medium Priority (Quality & Completeness)

### 3.1 Design System Token Alignment
- [ ] **[M-03]** Add Google Fonts to `index.html` as specified in DESIGN.md:
  - Inter, DM Sans, JetBrains Mono, Bricolage Grotesque (Google Fonts)
  - Clash Display (Fontshare CDN)
- [ ] Update `src/index.css` with CSS custom properties from DESIGN.md §6.2:
  - `--bg-primary: #030712`, `--bg-secondary: #0F172A`, `--bg-elevated: #1E1B4B`
  - `--accent-primary: #7C3AED`, `--accent-secondary: #F59E0B`, `--accent-soft: #818CF8`
  - All text tokens, border tokens, semantic tokens
  - `.aurora-bg`, `.glass-card`, `.gradient-border`, `.glow-violet` utility classes
- [ ] Update `tailwind.config.ts` to extend colors to expose CSS tokens as utilities
- [ ] Create `src/lib/utils.ts` with `cn()` helper (clsx + tailwind-merge)
- [ ] Install: `npm install clsx tailwind-merge`
- [ ] Apply `font-display: Clash Display` to hero title, `font-sans: Inter` to body

### 3.2 Event Cards — Fix Images + Add Fee Badges
- [ ] **[H-05]** Replace all `loremflickr.com` image URLs in `src/data/events.ts` with real or stable Unsplash URLs (specific photo IDs, not random)
  - Alternatively, use solid color gradient placeholders instead of external image services
- [ ] Update `src/components/EventCard.tsx` to display:
  - Registration fee badge (e.g., `₹99 / person`)
  - Team size badge (e.g., `Individual` or `Team of 4`)
  - "X spots remaining" or "Registration Closed" when slots fill (if slots_total is set)
- [ ] Add hover glow effect with CSS transition per TECH_RULES §6.2

### 3.3 Events Page Filters
- [ ] Add category filter tabs to `src/pages/Events.tsx`:
  - Tabs: All / Literary / Cultural / Informal / Management
  - Client-side filtering (no API call)
- [ ] Add search bar (fuzzy search on title + shortDescription)
- [ ] Add empty state when no events match filter

### 3.4 Event Detail Page Enhancements
- [ ] Update `src/pages/EventDetails.tsx`:
  - Switch from `import { events }` static data to `fetch('/api/events/:id')`
  - Add `GET /api/events/:id` endpoint in `server.ts`
  - Display structured metadata table: Date, Time, Venue, Category, Team Size, Fee, Prize
  - Add "Register for this Event" CTA that links to `/register?event=<id>`
  - Add Google Form iframe embed (from `google_form_url` field on event data) with fallback message
  - Add Social Share buttons: WhatsApp, Copy URL
  - Add Related Events row (same category, bottom of page)

### 3.5 Contact Form — Fix DB Persistence
- [ ] **[M-05]** Add contact form to `src/pages/Contact.tsx` (currently shows FAQs only — no submission form)
- [ ] Create `POST /api/contact` endpoint in `server.ts` that inserts into `contact_submissions` table
- [ ] Add Google Maps embed for venue (replace the gray placeholder div in Contact.tsx)
- [ ] Fix all placeholder contact info: email `info@voxera.com`, phone `+1(555)` → update to real values from settings API

### 3.6 Admin Dashboard Upgrades
- [ ] Add `Dashboard Overview` tab to `src/pages/Admin.tsx`:
  - Total registrations count, revenue sum (from `fee_amount` in registrations table)
  - Per-event breakdown table
  - Recent 10 registrations feed
- [ ] Add search + filter to Registrations tab:
  - Search by name, email, registration ID
  - Filter by event, payment_status
  - Real registration_code column (VX-2026-XXXX format)
  - Payment status column: `pending / confirmed / failed`
- [ ] Add `payment_status` and `registration_code` to CSV export
- [ ] Add Settings tab with: maintenance mode toggle, announcement banner text
- [ ] Add slot fill progress bar to Events tab per event card
- [ ] Extend event edit modal to include new fields: `registration_fee_single`, `registration_fee_team`, `team_size_min`, `team_size_max`, `slots_total`, `google_form_url`

### 3.7 Server — Middleware & API Standards
- [ ] Split `server.ts` monolith into separate route files (events, registrations, auth, webhooks, admin)
- [ ] Add Zod validation middleware for all POST/PUT endpoints
- [ ] Add `express-rate-limit` to `/api/auth/login` and `/api/register` endpoints
- [ ] Ensure all API responses follow the standard shape: `{ success: true, data }` or `{ success: false, error }`
- [ ] Add `Cache-Control: public, max-age=60` header to `GET /api/events`
- [ ] Install: `npm install zod express-rate-limit`
- [ ] Add Winston logger: `npm install winston`, create `server/utils/logger.ts`
- [ ] Add auth middleware to all `/api/admin/*` and `/api/registrations` routes

### 3.8 App.tsx — Code Splitting + Route Constants
- [ ] Convert all page imports to `lazy(() => import(...))` with `<Suspense>` wrapper
- [ ] Create `src/constants/routes.ts` with `ROUTES` constant object
- [ ] Replace all hardcoded route strings with `ROUTES.*` constants
- [ ] Wrap routes in `<AnimatePresence mode="wait">` for page transitions

### 3.9 Google Forms Phase 1 Integration
- [ ] Add `google_form_url` field to each event's data in the DB seed
  - Debate: `https://forms.gle/Tu7nF2xuPe6bGG1aA`
  - Poetry: `https://forms.gle/gRye4xaCtEpuXWLc7`
  - Pitch Perfect: `https://forms.gle/B5xCvuGFv33ZDaS8A`
  - Open Mic: `https://forms.gle/eKtGg9opFuhR4sm2A`
- [ ] Create `POST /api/webhooks/google-forms` endpoint that accepts Apps Script webhook POSTs and stores them in `google_form_submissions` table
- [ ] Add `WEBHOOK_SECRET` to `.env.example`

---

## 🔵 PHASE 4 — Analytics, Accessibility & Polish

### 4.1 Analytics
- [ ] **[M-04]** Add Google Analytics 4 tracking snippet to `index.html` (or via react-ga4)
- [ ] Create `src/lib/analytics.ts` with wrapper functions for key events:
  - `trackPageView()`, `trackRegistrationStart(eventId)`, `trackPaymentComplete(eventId, amount)`
- [ ] Install: `npm install react-ga4`

### 4.2 Animations Upgrade
- [ ] **[M-02]** Add `staggerChildren` character-by-character hero title reveal in Home hero (Framer Motion)
- [ ] Add scroll-triggered counter animation for Stats Strip
- [ ] Wrap all page transitions in `<AnimatePresence>`
- [ ] Add `prefers-reduced-motion` CSS rule in `index.css`
- [ ] Add CTA button shimmer sweep `::after` keyframe animation

### 4.3 Accessibility
- [ ] **[M-07]** Add `aria-label` to all icon-only buttons (hamburger, close, social icons)
- [ ] Add `role="navigation"`, `aria-expanded`, `aria-controls` to Navbar
- [ ] Ensure all form inputs have associated `<label>` elements
- [ ] Add visible focus rings: `focus-visible:ring-2 focus-visible:ring-violet-500`
- [ ] Ensure all interactive elements have `min-h-[48px]` for touch target compliance

### 4.4 Environment & Deploy Prep
- [ ] Update `.env.example` with all required variables (fill in from PRD §7.5)
- [ ] Add `.env` to `.gitignore` (already present — verify)
- [ ] Add `public/robots.txt` (allow all crawlers)
- [ ] Create `vercel.json` with rewrite rule: `{ "rewrites": [{ "source": "/(.*)", "destination": "/" }] }` for SPA routing
- [ ] Add `public/favicon.ico` (replace missing/placeholder)

---

## ✅ Already Working (Do Not Break)

- Navbar structure with scroll detection and active link indicator — `Navbar.tsx`
- Admin panel tabs UI and basic CRUD for events — `Admin.tsx`
- EventCard component rendering with category badge — `EventCard.tsx`
- Register form pre-populated from `?event=` query param — `Register.tsx`
- Team member add/remove in registration form — `Register.tsx`
- Settings API (GET + POST key-value store) — `server.ts`
- Event seeding from `src/data/events.ts` — `server.ts`
- `registration_enabled` check before accepting registration — `server.ts`
- Layout wrapper with Navbar + Footer layout structure — `Layout.tsx`, `Navbar.tsx`
- Framer Motion hero animation on Home page — `Home.tsx`
- `AnimatePresence` on mobile nav toggle — `Navbar.tsx`

---

## 📋 Dependency Install Summary

Run all at once (after confirming with team):

```bash
# Backend
npm install bcryptjs jsonwebtoken zod express-rate-limit winston razorpay resend drizzle-orm pg
npm install -D drizzle-kit @types/bcryptjs @types/jsonwebtoken @types/pg

# Frontend
npm install react-helmet-async react-ga4 swr axios zustand react-hook-form @hookform/resolvers zod clsx tailwind-merge
```

---

## 📁 New Files to Create

| File | Purpose |
|------|---------|
| `src/pages/AdminLogin.tsx` | Server-authenticated admin login page |
| `src/pages/About.tsx` | Club info + team page |
| `src/pages/NotFound.tsx` | 404 branded error page |
| `src/pages/RegisterSuccess.tsx` | Post-payment confirmation screen |
| `src/components/ProtectedRoute.tsx` | Admin auth guard |
| `src/store/authStore.ts` | Zustand auth state |
| `src/hooks/useCountdown.ts` | Live countdown to March 16 |
| `src/constants/routes.ts` | Route path constants |
| `src/constants/fees.ts` | Client-side fee display reference |
| `src/lib/utils.ts` | `cn()` class merger utility |
| `src/lib/seo.ts` | Helmet SEO helper |
| `src/lib/razorpay.ts` | Razorpay modal helper |
| `src/lib/analytics.ts` | GA4 event tracking wrappers |
| `server/utils/fees.ts` | **Authoritative** server-side fee calculator |
| `server/utils/logger.ts` | Winston structured logger |
| `server/utils/codes.ts` | Registration code generator (VX-2026-XXXX) |
| `server/services/email.service.ts` | Resend email service |
| `server/services/razorpay.service.ts` | Razorpay SDK wrapper |
| `server/middleware/auth.middleware.ts` | JWT verification middleware |
| `server/middleware/validate.middleware.ts` | Zod validation middleware |
| `server/middleware/rateLimiter.ts` | Rate limiting middleware |
| `server/db/schema.ts` | Drizzle ORM schema |
| `server/db/index.ts` | PostgreSQL connection pool |
| `public/robots.txt` | SEO crawl directive |
| `public/sitemap.xml` | Sitemap for search engines |
| `public/og-banner.jpg` | Social share banner image (1200×630px) |
| `vercel.json` | SPA routing rewrite for Vercel |
